# FastAPI service dependencies. Chains to the core library's requirements
# since api/ imports from photo_search.
#
# Install:  pip install -r api/requirements.txt    (run from photo-search/)
-r ../requirements.txt
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

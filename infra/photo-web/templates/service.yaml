apiVersion: v1
kind: Service
metadata:
  name: {{ include "photo-web.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "photo-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "photo-web.selectorLabels" . | nindent 4 }}
